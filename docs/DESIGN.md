# 設計メモ（要約）

真実の源は Notion **「ANRサンプルアプリ（DorodoroTimer）設計メモ」**（DroidKaigi 2026 登壇 → 【ANR】ページ配下）。
ここはリポジトリ内の要約スナップショット。

## 位置づけ
- DroidKaigi 2026 ANR セッション「あなたのANRはどこから？」の登壇用サンプル兼、将来のリリース製品の種。
- 普通のアプリ（集中タイマー）のコードに ANR の種を自然に埋め込み、マーカーコメント＋README で事例に紐づける。

## 主要な設計判断
- **demoMode**: debug トグルで ON/OFF。分岐は原則 Koin の実装差し替え、無理な所だけ局所タグ付き分岐。リリースは OFF。
- **DI = Koin**: `lazyModule` を ②③⑦（初期化集中・ClassLoader）の共通処方として使う。
- **永続化 = Room + SQLDelight 併用**: 「ライブラリがスレッドを管理してくれるか否か」を 1 アプリ内で対比（事例①の核）。
- **機密配慮**: 業務事例はメカニズムのみ具体・アイデンティティは伏せる。trace は `com.example.app` 置換の再構成版。

## 収録パターン
- ベース: CFP 事例 ①〜⑦。
- 追加候補（CFP外・公式 ANR doc 由来）は Notion バックログに記録。採否は後日。

## 今日のゴール（達成）
- Notion 整理 ＋ プロジェクト骨格。ANR パターン本体は後日。
